# NOT a secret — public backend base URL for the deployed API.
# Local dev: leave empty (Vite proxies /api to http://localhost:3001).
# Production (GitHub Pages): set to your deployed backend, e.g. https://call-agent-backend.vercel.app
VITE_BACKEND_URL=
